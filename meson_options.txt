option(
  'glibc_loader_name',
  type: 'string',
  value: '',
  description: 'Override the installed glibc loader-stub filename (for example ld-linux-x86-64.so.2)'
)

option(
  'musl_linker_path',
  type: 'string',
  value: '',
  description: 'Override the musl ELF interpreter path used by the glibc loader stub'
)

option(
  'glibc_runtime',
  type: 'combo',
  choices: ['auto', 'enabled', 'disabled'],
  value: 'auto',
  description: 'Build the glibc binary-runtime bridge (auto enables only verified x86_64 LP64)'
)
